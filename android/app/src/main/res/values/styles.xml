<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!--
          Required by androidx.core.splashscreen (Phase 6's cold-start
          fix, MainActivity's installSplashScreen() call): without this,
          the activity stays on this non-AppCompat Theme.SplashScreen
          after the splash-screen library's own setup runs, and
          Capacitor's BridgeActivity (an AppCompatActivity) crashes in
          onCreate() with "You need to use a Theme.AppCompat theme (or
          descendant) with this activity." This tells the library which
          real theme to switch the activity to right before onCreate()'s
          own AppCompat checks run.
        -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>